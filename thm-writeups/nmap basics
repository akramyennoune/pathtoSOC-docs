completed the nmap basics room and the networking module in cyber security 101 path on tryhackme and here is what the nmap room contained:
understood what is the network mapper
basic commands like: -sN -sL -sT -sS -sU
how to limit scans by the -p command and -f scans the most common port till 100
service detection with the -A command 
-Pn command to scan hosts that appear to be down
also the timing template with the -t command and how paranoid or aggressive it can scan
finally on how to save the scan into a file we use the -oA to get all the outputs in all major formats
this room was only introducion yet it was huge for someone who is eager to learn
